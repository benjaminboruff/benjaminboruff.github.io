import { Button } from 'reactstrap';
import Layout from '../components/layout';

const Home = () => {
  return (
    <div>
      <Layout>
        <Button color="danger">Danger!</Button>
        My site reboot!
      </Layout>
    </div>

  );
}

export default Home;
